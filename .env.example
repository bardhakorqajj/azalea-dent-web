# ---------------------------------------------------------------------------
# Copy to `.env.local` for development, and set the same keys in your hosting
# provider for production. Never commit `.env.local` — it is git-ignored.
# ---------------------------------------------------------------------------

# Canonical origin, used for metadata, hreflang, sitemap.xml and robots.txt.
# Set this to the live domain once it is connected, e.g. https://azaleadent.com
# On Vercel it falls back to the project's production URL automatically.
NEXT_PUBLIC_SITE_URL=

# ---------------------------------------------------------------------------
# Appointment delivery — pick ONE of the two options below.
# With neither set, /api/appointment answers 501 and the form tells the patient
# plainly that nothing was sent, offering WhatsApp and Instagram instead.
# ---------------------------------------------------------------------------

# Option A — email via Resend (https://resend.com, free tier is enough).
RESEND_API_KEY=
# Where appointment requests are delivered.
APPOINTMENT_TO_EMAIL=
# Sender address. Must be on a domain verified in Resend.
# Defaults to "Azalea Dent <onboarding@resend.dev>", which only delivers to the
# Resend account owner and is for testing only.
APPOINTMENT_FROM_EMAIL=

# Option B — POST the request as JSON to any endpoint
# (Zapier, Make, n8n, a Google Sheet webhook, a clinic CRM…).
APPOINTMENT_WEBHOOK_URL=
